# min(a, b, c) and max(a, b, c) functions
# abs() it's module
# pow(x, y) returns value x^y

import math
x = math.sqrt(64)
print(x) # output 8.0

a = math.ceil(1.4) # a = 2
b = math.floor(1.4) # b = 1

p = math.pi
print(p)